{% extends 'base.html.twig' %}

{% block body %}
    <article class="page">
        {% include 'components/_fil_ariane.html.twig' %}

        <h1>{{ page.title }}</h1>
        <div class="page__contenu">
            {{ page.html|raw }}
        </div>
    </article>
{% endblock %}
